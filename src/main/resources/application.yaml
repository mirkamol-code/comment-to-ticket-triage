server:
  port: 8080
  servlet:
    context-path: /api/v1
spring:
  application:
    name: comment-to-ticket-triage
  datasource:
    username: mirkamol
    password:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/comments_db
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: false
        show_sql: false
ai:
  huggingface:
    model: "meta-llama/Llama-3.3-70B-Instruct:groq"
    token: ""